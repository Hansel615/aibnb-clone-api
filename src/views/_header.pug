header

  nav
    a(href="/")
      img(src='https://cdn.1min30.com/wp-content/uploads/2018/03/Airbnb-Logo-1.jpg')
  nav
    
    if user 
        if user.role === 'user'
            .navuserpic
              a.sign.host(href='/me/host') Host Your Home
              a.sign.logout Logout
              a.nav__el(href='/me')
                span= user.username.split(' ')[0]
                
        else 
            .navuserpic
              a.sign.logout Logout
              a.nav__el(href='/me')
                span= user.username.split(' ')[0]
                

    else
        a.sign(href='/signup') Signup
        a.sign1(href='/login') Login